name = "ai-chat-worker"
main = "index.js"
compatibility_date = "2024-01-01"

# Environment variables (set in Cloudflare dashboard or use wrangler secret)
# Run: wrangler secret put GROQ_API_KEY
# Run: wrangler secret put OPENAI_API_KEY

[vars]
# Public variables (non-secret)
ENVIRONMENT = "production"
